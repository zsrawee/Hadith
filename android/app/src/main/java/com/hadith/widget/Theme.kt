package com.hadith.widget

import androidx.compose.material3.darkColorScheme
import androidx.compose.ui.graphics.Color

/** Custom color scheme matching the Islamic design language of the web app. */
val WidgetColors = darkColorScheme(
    primary = Color(0xFFC9A84C),          // Gold
    secondary = Color(0xFF1A7A6B),        // Teal
    tertiary = Color(0xFF4A90D9),         // Accent blue
    background = Color(0xFF0A1F2E),       // Navy
    surface = Color(0xFF1A3A4A),          // Lighter navy
    onPrimary = Color(0xFF0A1F2E),
    onSecondary = Color.White,
    onBackground = Color(0xFFF5EDD6),     // Cream
    onSurface = Color(0xFFF5EDD6)
)
